ifdef CROSS_COMPILE
	ifeq ($(CROSS_COMPILE),aarch64-none-linux-gnu-)
		CC = aarch64-none-linux-gnu-gcc
	else
		CC = gcc
	endif
else
	CC = gcc
endif

all:
	#############################################################
	# Make 'All' using CC                                       #
	#############################################################
	$(CC) -o writer writer.c
	#############################################################
	# Built 'writer' executable using 'aarch64-none-linux-gnu'! #
	#############################################################
default:
	##############################
	# Make Default               #
	##############################
	gcc writer.c -o writer
	##############################
	# Built 'writer' executable! #
	##############################


clean:
	##################################################################
	# Make Clean                                                     #
	##################################################################
	rm -f writer
	rm -f writer
	rm -f *.o
	##################################################################
	# Removed all '*.o' files and the main 'writer' executable file. #
	##################################################################

CROSS_COMPILE:
	#############################################################
	# Make Cross-Compile                                        #
	#############################################################
	$(CC) writer.c -o writer
	#############################################################
	# Built 'writer' executable using 'aarch64-none-linux-gnu'! #
	#############################################################
